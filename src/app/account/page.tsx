import Account from "@/components/common/Account/Account";
import React from "react";

const Page = (res: any) => {
  return (
    <div>
      account page
      <Account />
    </div>
  );
};

export default Page;
